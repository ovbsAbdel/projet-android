<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!-- MainActivity declaration (already present) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ScoresReviewActivity declaration -->
        <activity
            android:name=".ScoresReviewActivity"
            android:exported="true">
            <!-- You can add intent filters if needed -->
        </activity>

        <!-- Other activities if any -->
        <activity
            android:name=".SudokuGameActivity"
            android:exported="true">
            <!-- Add any intent filters if needed -->
        </activity>


    </application>
</manifest>